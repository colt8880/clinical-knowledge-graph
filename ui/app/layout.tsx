export const metadata = {
  title: "Clinical Knowledge Graph",
  description: "Explore and evaluate clinical guidelines",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
